<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.SmartHire.hrService.mapper.JobSkillRequirementMapper">

    <!-- 根据职位ID查询技能名称列表 -->
    <select id="selectSkillNamesByJobId" resultType="java.lang.String">
        SELECT skill_name
        FROM job_skill_requirement
        WHERE job_id = #{jobId}
        ORDER BY is_required DESC, created_at ASC
    </select>

    <!-- 根据职位ID删除所有技能要求 -->
    <delete id="deleteByJobId">
        DELETE FROM job_skill_requirement
        WHERE job_id = #{jobId}
    </delete>

</mapper>
