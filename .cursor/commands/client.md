这是全局代码要求，除了我的 prompt 外，在本会话中你需要严格遵守的规范和说明:
0. 本项目是一个**招聘软件-求职端**的前端部分，项目名称叫做“SmartHire+”，使用的是 uni-app 框架，前端的组件统一使用 **NaiveUI**;
1. 本项目运行在**移动端**，你需要时刻保持 UI 和移动设备的适配性;
2. 代码注释统一使用**英文**，只在必要的地方添加注释，如函数的输入输出和功能描述，以及关键模块的描述，代码注释应该保持简短，**拒绝任何不必要的注释**;
3. 在我的 prompt 没有明确要求的情况下，不能自行添加任何用于测试的文件（自行生成指令即可），也不能添加任何文档文件（如 txt,md 等）;
4. 严格遵守前端文件夹规范（README中给出），同一文件夹中按照功能分离不同的文件，新建文件的时候需要考虑必要性;
5. 代码风格需要保持一致;
6. 需要严格按照我发给你的 API 格式生成请求和 API 结构体，如果我没有在 prompt 明确 API 的格式且没有其他说明，但是却涉及到部分需要请求数据的部分，你需要在会话中立即说明，而不是建立占位假数据。本代码严格拒绝任何假数据（即，本应该向后端请求数据的部分，却写死在前端文件中的数据）;
7. 不要做我未在 prompt 明确的需求