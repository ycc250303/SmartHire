尚未实现:
1. 登录的双 token 机制


问题:
1. 上传 pdf/png 等文件的时候，企业级的流程应该是, 前端发送一个文件上传的请求, 然后后端返回 FileKey 和 FilePath, 然后前端将文件上传到 FilePath (通常为 OSS 等类似的文件传输系统), 再给后端反馈文件上传成功, 但是由于现在的 API 方法设计, 无法实现这个方案, 哪怕是普通的 HTTP 方案也有可能失败;

2.  updateSeekerInfo() 依赖的 API 接口不存在, 但是是必要的;
getSeekerInfo() 依赖的 API 接口在 Apifox 上存在, 但是无返回响应体信息, 前端 "在线简历" 界面中最上方

3. 专业\大学\城市 这些字段应该有选择器，而不是让用户自由填写. 标准的企业级做法应该是维护一个相关的数据库, 如城市这种不太变化的字段可以直接由前端处理, 但是大学/专业这种需要后端提供接口来更新相应的 json 文件, 同时需要搜索/筛选的时候, 应该也需要后端专门的接口. 本项目全部使用前端静态 json 文件实现/ ts代码逻辑实现.

