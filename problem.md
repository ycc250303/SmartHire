尚未实现:



问题:
1. 上传 pdf/png 等文件的时候，企业级的流程应该是, 前端发送一个文件上传的请求, 然后后端返回 FileKey 和 FilePath, 然后前端将文件上传到 FilePath (通常为 OSS 等类似的文件存储系统), 再给后端反馈文件上传成功, 但是由于现在的 API 方法设计, 无法实现这个方案, 哪怕是普通的 HTTP 方案也有可能失败;

2.  updateSeekerInfo() 依赖的 API 接口不存在, 但是是必要的;
getSeekerInfo() 依赖的 API 接口在 Apifox 上存在, 但是无返回响应体信息, 前端 "在线简历" 界面中最上方

3. 专业\大学\城市 这些字段应该有选择器，而不是让用户自由填写. 本项目全部使用前端静态 json 文件实现/ ts代码逻辑实现. 但是否符合标准做法有待讨论

